{
  "name": "traderframe-connector-mcp-server",
  "version": "1.0.1",
  "description": "MCP server that sends buy/sell trading signals to a configured webhook (position-aware).",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf server",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
